Continue from your independent review saved at docs/interface-missions/claude-review.md. User explicitly authorized UI and mission implementation; lead has compared current mechanics and owns mission model/persistence + MissionBoard component.
Implement BUILD TOOL ACCESS and compact HUD now. Own ONLY src/ui/Hud.tsx, new src/ui/BuildPalette.tsx, new src/ui/buildPalette.css. No other file writes, no commits. Read docs/IMPLEMENTATION-LESSONS.md first. Existing changes must be preserved (showTips state, separated MapControls). New brief supersedes any older direction in CLAUDE.md. No external image tools or new artwork needed.
Chosen starting layout: all7 buildings + Road/Remove readily discoverable, remove reliance on More buildings modal for normal selection. Your two rows of4 (Home/Store/Park/Road then Police/Fire/Clinic/Remove) plus compact row for Stops/Lights/Divert/Rotate/Pause is sensible, but at320px font17.6px cannot have huge words in5 cols; use short visible labels and full accessible names, and if necessary move rotate/pause context elsewhere. Keep title/cost/selection clear, min44px tap targets, cost source COSTS not hardcoded. Selected button aria-pressed, shortcuts preserved. Can use approved atlas for icons if helpful but not necessary. All tools always available. Invalid/unaffordable tools explain available cost rather than hard gating. Selected tool and actual game feedback must both remain readable (don't hide failed placement). Keep pan controls, pause, report, menu. Build UI must not swallow map or leak taps through; .city-header/.city-controls are measured by ResizeObserver.
Lead provides src/ui/MissionBoard.tsx default component, renders compact one/two-line current-mission button and opens its own dialog. Import and insert <MissionBoard /> in header. It is not yet written; compile after lead integrates. Aim top+bottom allow at least210px playable map on320x640 and300px on390x844. Trim duplicated always-on stats into City report if needed; keep funds, current vehicles/traffic, emergency indicator, meaningful mission summary. No new tiny font overrides:17.6px floor for new controls/content. Preserve s.showTips preference.
Use your judgement on layout tradeoffs and report them. Main mission early steps will be actual first visit, multiple households served, park use, growth; lead rejects early throughput-per-minute demands since user explicitly says efficiency comes later. No budget changes or mission cash payouts this pass; manager will urge inexpensive growth but player foresight always allowed. This work is NOT full tutorial/external city system.
Return actual file deliverables and concise handoff, reusable lessons. Do not run live browser while source owners still editing. If needed npm commands run inside nix develop. You can typecheck once lead signals modules ready, but don't overwrite stubs into MissionBoard.
