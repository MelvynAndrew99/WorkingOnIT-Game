Your UI implementation is delivered and lead browser functional checks pass all tools, rotation, actual map construction and claim/reload at320/390/1440. Please make bounded responsive polish fixes in your UI scope (no model/store/scene). User approved source access remains.

Actual local screenshot findings described in text (do NOT read/upload images): at320x640 funded fixture header shows '$9,…' truncation instead of useful fund amount; claim objective instruction gets squeezed to about70px column and wraps7lines alongside buttons; Places shelf Remove overlaps/clips because rotate consumes a fifth slot. Fix small-screen composition respecting44px targets and17.6px body; compact funds format is fine, rearrange action/rotate alignment rather than shrink text. Maparea currently243x241 at320,311x497 at390,932x729 at1440wide. Preserve your design and colors. Browser harness files/verification notes in your handoff directory may be read as text, do not run tools outside Read/Glob/Grep/Edit/Write.

Also include s.tutorial?.waived?.reason in emergencyObjective as note, because emergency currently replaces the tutorial and hides the mayor's rescue grant. BuildPalette dynamic prices already work. Lead already integrated STARTING_FUNDS store default, free construction preview and responsive rail viewport + dynamic observer. Those requested tasks are done.

Read docs/claude-ui-handoff/lead-result.md and docs/economy-rework/contract.md. Grok still owns modelfiles. Deliver concise followup-result.md with fixes; don't restart broad redesign or change architecture. No commit/deploy.
