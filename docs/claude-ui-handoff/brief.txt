SOURCE ACCESS: User explicitly approved the exact text-source scope listed in docs/economy-rework/source-access.md. Read that scope first and access only those approved files plus your own new permitted code/notes. Do NOT read images, credentials, environment files, private saves or unrelated files. This overrides any earlier suggestion to inspect screenshots.

User explicitly hands UI design/layout ownership to Claude Code. Latest exact feedback: colors are liked, layout is horrible. Lead must stop designing UI and let Claude rethink it. User also asks lead to add toggle desktop browser wide resolution vs existing mobile portrait. You are the UI layout owner now; implement your best layout, not just advice.

Read AGENTS.md, docs/DESIGN.md, docs/IMPLEMENTATION-LESSONS.md, CLAUDE.md and docs/interface-redesign/README.md before work. Prior screenshot evidence docs/interface-redesign/*.png may be inspected locally (do not send images externally). Preserve approved sprites and navy/cyan/yellow color direction. Tutorial must stay visible during actual construction, be contextual, skippable, and never require a modal for routine teaching. Missions show claimable rewards; existing exactly-once receipts and tutorial/traffic models stay intact. All build/traffic tools accessible, emergency instruction appropriate, real map area dominant. Challenge previous layout choices independently; user rejected stacked current layout. Do not merely shrink fonts or remove useful feedback. Honor 44px touch targets and 17.6px body text. Design desktop AND phone layouts.

Your permitted runtime files: src/ui/Hud.tsx, BuildPalette.tsx, buildPalette.css, TutorialCoach.tsx, tutorialCoach.css, MissionBoard.tsx, missionBoard.css, TutorialPanel.tsx, gameInterface.css, MapControls.tsx, mapControls.css, CityDialogs.tsx, cityDialogs.css. May add UI-only components/styles. NO edits to MainMenu.tsx, App.tsx, src/styles/app.css, src/state/store.ts, src/game/*, or audio: lead owns display setting/frame/stage. No game balance, missions model, art, publication or external tools.

Lead adds AppState displayMode:'auto'|'wide'|'portrait', local preference; #app-frame data-display-mode attribute. Auto uses full browser width >=900px landscape, portrait otherwise; wide full available width; portrait classic phone frame. Menus/title stay portrait; chosen layout applied while playing. Pixi stage expands logical viewport width on desktop rather than enlarging all sprites. Frame change is live and observed. You can key your UI container queries/responsive composition to actual #app-frame width (wide desktop uses larger frame). Avoid viewport-width-only assumptions because users can force portrait on desktop.

Renderer currently reserves top .city-header height +6px and bottom .city-controls height +6px, full width minus14designunits on each edge. Lead can extend viewport to account for side panels if you need: propose your required DOM classes and bounding-box contract ASAP in docs/claude-ui-handoff/contract.md before implementing panels. Default if staying top/bottom preserve these measured wrappers. Native dialogs block map input correctly; nonmodal panels must claim their own pointer events, map hit area must exclude panel rectangles. Lead will integrate viewport adjustments you request.

LATEST USER CORRECTION: Grok is assigned economy/model work. Tutorial should earn money and start below 10K. Remove Free Help / worked example buttons from normal tutorial UI; user meant zero-cost objective construction, not auto-built solutions. After sustained lack of progress the mayor can waive costs for the CURRENT stalled objective. Clinic was just an example, not a special Clinic-only feature. Grok will publish model/UI API in docs/economy-rework/contract.md; read and use when available. Do not write game model files. Keep optional teaching district distinct from Free Help and discuss in handoff if needed.

Deliver working UI files, docs/claude-ui-handoff/result.md explaining layout rationale/tradeoffs, actual changed files and tests/known limits, and reusable lessons. Read-only reports/notes in this handoff folder permitted. Do not claim verification you did not run. Lead runs nix build/browser tests. Source files outside your ownership are being modified concurrently. Do not commit or deploy.
