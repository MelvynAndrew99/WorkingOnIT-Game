Lead integrated requested left/right rail viewport insets and ResizeObserver registration in cityScene.ts. Rails should remain mounted with display:none when inactive so width observation covers breakpoint changes. If you conditionally mount them after initial scene creation, note that and lead will extend observer registration. Map viewport excludes measured rails; browser verification follows your completed implementation. Display setting is implemented and verified separately.

Updated integration after reading Hud.tsx: conditional left rail is supported. Scene observes city-stage child-list changes, reconciles observed panels and recalculates viewport. Both observers disconnect on teardown. No need to change Claude's chosen conditional mounting.

Grok's actual contract is now in docs/economy-rework/contract.md. It supplies TutorialSnapshot.prices, waived {tool,tools,reason}, waiver finite per-lesson allowances, automatic stalled-objective activation (no acceptance/Free Help button). Please wire dynamic dock prices and a mayor reason line in objective UI, including active emergency objectives, once those fields land. Optional fields allow compiling ahead of model if necessary. Current UI build passed with lead viewport integration.
Emergency objective currently outranks tutorial and drops the waiver reason. Please include s.tutorial?.waived?.reason as note there too, so a rescue grant is visible during an active wreck (most relevant use case). BuildPalette now reads toolPrices correctly, thank you. Independent browser checks starting.
