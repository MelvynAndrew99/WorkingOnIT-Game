Implement ONE verified routing slice for Working ON IT!, a persistent traffic-efficiency game. Read shared implementation lessons and docs/traffic-world/routing-plan.md. Preserve existing saves, original trip goals, real entrances, lane reservations, emergency scene work and safe returns. Reuse current roads, sprites and debug UI. No mockups at any stage. Scenario and visual variations come last, after mechanics.

Jam direction: shared dynamic road-cost snapshots (travel time + bounded measured queue/control delay), stable congestion-aware rerouting with cooldown/improvement thresholds, and explicit emergency versus ordinary policies. Keep impossible roads as hard constraints; movement still owns occupancy safety. Explain route decisions through the live inspector. Use deterministic algorithms and bounded work, not per-car LLM calls.

The lead must specify ONE module boundary and its acceptance fixtures before implementation. Deliver that slice, relevant regression results and reusable lessons. No broad engine rewrite. A global observer diagnoses/suggests; automatic signal/lane/detour changes are a later proposed opt-in tool, not a silent change in player control. Preserve distinction between road congestion and full destinations. Keep prediction, multi-lane behavior, weather and broad optimization systems deferred until their prerequisites are proven. Do not describe proposed features as implemented.
