SOURCE ACCESS: User explicitly approved the exact text-source scope listed in docs/economy-rework/source-access.md. Read that scope first and access only those approved files plus your own new permitted code/notes. Do NOT read images, credentials, environment files, private saves or unrelated files. This overrides any earlier suggestion to inspect screenshots.

Implement the economy/tutorial-model correction for Working ON IT! as the actual Grok Build specialist. Read AGENTS.md, docs/DESIGN.md, docs/IMPLEMENTATION-LESSONS.md, CLAUDE.md and src/game/cityModel.ts, cityTutorial.ts, cityVisits.ts, cityMissions.ts plus tests first.

User: 'leverage grok to fix the economy, the tutorial should generate money so the incentive to place buildings that earn revenue increases. 10K is alot to start out with and the free help is not supposed to be a button it was to make the objective costs zero. So if they can't figure it out after a certain period of time, we can offer a hospital for free to get started from the mayor.'

LATEST CLARIFICATION: 'the free clinic is an example of a tutorial objective that is causing the player to not progress.' Implement assistance for whichever current objective has stalled, NOT a special Clinic grant. The mayor waives relevant objective construction costs after a sustained lack of progress; player still builds their solution. No Free Help button or automatic worked layout. Track meaningful progress, not merely button clicks; respect pauses and reloads. Timings and waiver limits are provisional. Avoid treating this clarification as an instruction to make every building free forever.

Implement smaller viable starting funds for NEW towns only, meaningful real visit revenue in tutorial, and zero-cost assisted objective construction through ordinary placement. Keep traffic optimization primary, generous experimentation, full refund of actual amount paid (free constructions cannot mint money by demolition). Preserve old saved balances and buildings. Avoid arbitrary money ticks dwarfing earned visits. Exact amounts/time are your provisional tuning: explain reasoning with budget examples. Do not silently add service upkeep. Tutorial should leave player with earned funds toward main game. Timed assistance should use saved progress/time, pause-aware, limited per objective, with real building placement/dispatch. No forced external traffic or fabricated accidents. Assistance must not reset on reload or duplicate grants/refunds.

You own ONLY src/game/cityModel.ts, cityTutorial.ts, cityVisits.ts, cityMissions.ts and their corresponding tests plus NEW src/game/cityEconomy.ts/tests if useful. No UI/store/scene edits. Actual Claude owns UI; lead owns scene integration. Write docs/economy-rework/contract.md ASAP with proposed callable price/grant APIs, TutorialSnapshot fields and actions for Claude/lead. Follow existing model->snapshot->store path. Keep backward-compatible optional additions where possible. Do not remove legacy action type support until consumers migrated. Read docs/claude-ui-handoff/brief.txt for UI ownership boundary. Do not change art, publish, commit, or use images/external research. Model balance is not player-verified; label assumptions.

Deliver actual code edits and docs/economy-rework/result.md with changed files, budget example, tests actually run, uncertainties and proposed reusable lessons. Run npm via nix develop if available; lead handles final build/tests. If shell/write execution fails, return exact proposed patch in result rather than repeatedly retrying or claiming completion. No subagents. Only approved project edits; no unrelated commands.
