I'll start from the approved source-access list, then read the economy/tutorial model files and write the callable contract before changing code.Source scope is locked. Next I’ll read the economy brief, design docs, and the model files I’m allowed to change.I’ll read the design docs and the four model modules plus their tests so the contract can match the real snapshot path.I have the economy rules; next I’ll read the model modules in full so the contract matches the real snapshot and action path.The current model still starts at 10,000 and refunds list prices. I’ll read tests, save/store, and how snapshots are consumed so the contract stays compatible.I have the snapshot path. I’ll publish the price/grant contract next, then implement against it.
