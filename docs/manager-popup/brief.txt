User explicitly assigns Grok creation of an in-game manager popup. Deliver two new self-contained files src/ui/ManagerPopup.tsx and src/ui/managerPopup.css plus docs/manager-popup/grok-result.md. Do not edit/read other UI files. Existing source scope remains docs/economy-rework/source-access.md; read AGENTS.md, docs/DESIGN.md, docs/IMPLEMENTATION-LESSONS.md and CLAUDE.md for context. No images, audio, credentials, private saves, network research, publication or unrelated files. Lead owns integration and browser verification. Use installed client edit tools, no shell needed.

Contract: default export ManagerPopup({open,onClose,onBuildRoad}: {open:boolean;onClose:()=>void;onBuildRoad:()=>void}). React19 TypeScript, import CSS. Native dialog showModal/close with accessible title, Escape dismissal, responsive 320px phone to desktop, keyboard focus and focus return, 44px touch targets. Parent owns all pause behavior; component never mutates simulation or store. Use navy/cyan/yellow with text speaker badge (no invented portrait/art). No runtime AI.

Copy: speaker City manager; headline More roads! Quote: “We need more roads! I knew this town had potential.” Follow: “Build a way around the wreck. Keep those customers moving. I'll take care of the credit.” Dry crew response: “And we'll still need access to help the people at the crash.” Clear objective instruction: connect roads on both sides of the blockage, then use a diversion if needed; keep emergency access. Do not promise every site is solvable or roads clear wrecks. Buttons “Plan a route” (onBuildRoad) and “Back to my city” (onClose). Show “Traffic is paused while you read.” Parent will ensure this. This is a user-opened briefing from emergency/current detour objectives, NOT automatic unsolicited crisis advice; that needs a future inactivity timer. Ignore objective / I have a plan is a later backlog feature, don't mark missions completed or implement it now.

The newly discussed H-road starter tutorial remains a design plan, not implemented by this task: existing roads first, growth brings real conflict, bypass/diversion, emergency services then prevention and outside connection. Do not silently gate existing tools or alter towns. Popup will be reusable in that later lesson. Include proposed reusable lessons and honest untested limitations in handoff. Complete actual code, not just a proposal.
