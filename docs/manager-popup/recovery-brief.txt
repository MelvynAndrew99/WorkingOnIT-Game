Create the code for a React19 TypeScript manager modal in our city/traffic game Working ON IT! Your prior installed-client run ended without writing files. DO NOT use any tools or read files. Return exactly two fenced code blocks, TSX then CSS, which the lead will save as src/ui/ManagerPopup.tsx and src/ui/managerPopup.css. The user specifically asks Grok to create this popup. Everything needed follows.

Default export ManagerPopup({open,onClose,onBuildRoad}: {open:boolean;onClose:()=>void;onBuildRoad:()=>void}). import './managerPopup.css'. Native HTML dialog with showModal/close, accessible title, Escape dismissal, focus restore, focus trap via native dialog, responsive 320px to desktop, 44px buttons. Parent owns pause and tool selection: no store imports, no timers, no external libraries beyond React. Navy/cyan/yellow, textual City manager badge, no portrait or other art. Scoped CSS class manager-popup to avoid global styles, max-height scrolling on short phones. Do not rely on global styles. UI has larger global button defaults so qualify own dialog selectors. No animations required.

Tone: sincere helpful manager eager for recognition; sometimes competent, sometimes order of priorities wrong, sometimes embellishes. Not always fumbling or knowingly selfish. More/bigger enthusiasm. Headline “More roads!” Quote “We need more roads! I knew this town had potential.” Follow “Build a way around the wreck. Keep those customers moving. Now that's an improvement people will notice!” Crew reminder “Keep an approach open for us. We've still got people to help.” Plain instruction connect roads on both sides of the blockage, use a diversion if needed, keep crew access. This is a suggestion not a guarantee roads alone solve all layouts. End “Traffic is paused while you read.” Buttons “Plan a route” calls onBuildRoad, “Back to my city” calls onClose. No Ignore or Later objective action here: tutorials cannot defer objectives. Later after leveling up players may defer pending objectives while finishing other work, not abandon them. Speech close never completes or defers anything. Popup user-opened from objective, not unsolicited spam. Export optional prop example?:boolean default false to show a short preface “Planning a future blockage? Here is the manager's approach.” if example true, for safe tutorial bypass lessons with no actual crash. Parent will set it.

Return complete TSX and CSS now, no plan or tool calls.
