**Verdict:** Keep the mission-in-list, recognition-only receipt, never-revoke, pause-freeze, and visit ≠ return ≠ pending UI. Do not ship the stacked time gates or `max(6, …)` as written. FLOW-01 original is evidence of a slow town that still serves every home; the proposed bar likely scores it as a fail.

**Evidence (given, not re-measured):** 180s same town — original 47 visits / 46 returns, min 3 returns/home, 553 wait-s; retime 69/68, min 7, 180 wait; nearby 96/93, min 7, 314 wait. No access/capacity fails. One-second qualification: original 40/180, retime 165, nearby 135, preplanned EW 180/180. No player-fun observation. Eval does not change the world.

**Already aligned with prior lessons:** high-water homes until earned; replacement via new service; outside-traffic consent; quiet ≠ Smooth; current counts; limiting reason; both-way routes; no shown timer or fail deadline; existing list, priority, and two layouts.

### Gates

**2 shopping returns / 60s / every home.** Original min 3/180s is about one return per 60s. Two-in-60s is stricter than a town that already returned every household three times with paired visits. **Hypothesis:** this plus the 20s stop explains 40/180, not “nobody is served.” Keep every household; loosen to **≥1 real return in 60s for every current home**, still fail any home with zero in-window returns.

**Stop >20s / unfinished known-age trip >60s.** Original 553 wait-s at NS-priority: **hypothesis, not proof,** that EW red often exceeds 20s (retime 165/180 with 180 wait-s). Twenty seconds is textbook LOS. Bound: longest **current** stop **>30s** in game words; keep 60s unfinished trip as the stuck-car gate.

**10s continuous hold.** Label debounce is already 4s. Ten seconds is a hidden countdown. EW waits for no reason. Original’s 40 samples may not contain 10 consecutive — **blocker if unproven:** a baseline that sometimes serves all may never receipt. Qualify from existing evidence when the snapshot is true; if you debounce, use the **same 4s as labels**.

**`max(6, current homes)`.** Forces growth below six homes. That is hidden growth. **Target = current occupied homes at first post-tutorial offer**, then high-water. Floor at one home, not six.

**Shopping-only in mixed-purpose towns.** **Hypothesis:** departure alternation makes shopping a fraction of trips, so two shopping returns/home/60s can fail a town that is serving other needs. Gate **returns that clear that home’s outstanding need**, not a shopping quota. Keep purposes distinct in Details.

### Demolition, mix, spare

High-water count blocks shrink-to-pass. Relocating a starved home next to a store is play: the new home still needs real returns. Do not freeze parcel IDs or lock demolition.

Do not add a spare-capacity hard gate. If pending needs exist and spare is unreachable, show that as the limiting reason. Nearby vs retime: extra stores raised visits but not min-returns (both min 7) and raised wait (314 vs 180). Prefer “homes without returns” over total visits so players do not overbuild.

### Blockers

- No proof original can hold 10 consecutive qualifying seconds.
- No fun-play evidence for Smooth/Crawling or mission juice.
- Shopping-2-in-60s + 20s stop + 10s hold + max(6,…) stacked: high risk the “sometimes serving” baseline cannot earn.

Do not add an event mode, countdown, or hidden growth. Normal needs stay on after success.
