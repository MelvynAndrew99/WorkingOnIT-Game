Implement only the bounded FLOW-02 UI in Working ON IT! Lead implements model/store/scene, so do not edit those. Read docs/IMPLEMENTATION-LESSONS.md and the allowed existing UI files first. No delegation, web, images, private saves, credentials or unrelated files. Read only AGENTS.md, CLAUDE.md, docs/DESIGN.md, docs/IMPLEMENTATION-LESSONS.md and src/ui TSX/CSS, src/state/store.ts. Source access to those is already user approved. Preserve existing uncommitted work.

Task: current-objective compact flow feedback and Dashboard detail. No new Jobs/history page or modal. Keep existing tutorial/claim/emergency/outside-invitation precedence. No countdown, revoked receipts, mandatory solution button or simulation change. No tiny type: 1.1rem minimum, buttons >=44px, navy/cyan/yellow. Desktop and narrow only. No aria-live for polled counts.

Lead's coming contract: AppState.flow nullable object with fields label:string (stable model label), reason:string, visits:number, returns:number, pendingNeeds:number, waitingVehicles:number, accessLimitedHomes:number, capacityLimitedHomes:number, homesWithoutReturn:number, qualifiedHomes:number, targetHomes:number, earned:boolean, selectedRoad: null | {x:number,y:number,vehicles:number,waiting:number,longestStop:number,label:string}. Mission id 'neighborhood-flow', title 'A neighborhood worth visiting', existing MissionItem supplies target/current/task etc. Labels scope neighborhood shopping, not professional LOS. Metrics visits/returns refer to recent 60 simulated seconds. Pending needs are capped existing demand including unfinished visits. Selected road is selected by tapping existing road with Road tool; no placement change.

Allowed writes ONLY src/ui/FlowFeedback.tsx (new), src/ui/flowFeedback.css (new), src/ui/ObjectiveBar.tsx, src/ui/CityDialogs.tsx. Lead handles scene and store. Implement compact flow line in objective note area when job.id==='neighborhood-flow', plus FlowFeedback detail component in that objective's Details and near top of Dashboard. Add selected road information to that component when available. Other jobs should not get extra height. For this job primary action should show Traffic view (store diagnosticView='traffic', tool='road', panning=false, toolSelection++) instead of prescribing construction; label 'Inspect traffic'. In Details mention alternatives roads/controls/nearby stores and no countdown. Dashboard should show current neighborhood service even after earned, separately saying recognition retained if earned, not suggesting current flow is guaranteed.

Please implement actual files, give concise handoff and proposed reusable lesson. Do not run build/test/npm (lead handles). Do not edit docs or commit. Do not read model files outside allowed scope; the complete contract is above. If blocked by client budget, say so and stop.
