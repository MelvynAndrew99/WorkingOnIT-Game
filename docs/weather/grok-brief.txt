The user explicitly asks: "Can you ask grok to build a weather system for my game?" You are the requested installed Grok implementer. Implement a bounded, working first weather system in this isolated snapshot. Read AGENTS.md and docs/IMPLEMENTATION-LESSONS.md first. This request supersedes older weather-deferred notes.

Default first slice: visual atmosphere with slowly cycling clear / cloudy / rain, gentle transitions, restrained pixel-art-friendly map shading and rain. No lightning flashes. Preserve readability of cars, markings, construction previews and emergency indicators. Weather follows simulation pause and is clipped to the map; does not cover HUD/dialogs. Offer a simple saved Weather on/off preference in existing Settings, enabled by default; respect reduced motion with static atmosphere. No traffic speed/safety/economy/demand changes, missions, active city save changes, audio, publication, profiling or performance tests. No runtime AI, external services or dependencies. Bounded pooled rendering, no full-map tile scans or per-frame texture rebuilding.

You may read source in this snapshot. Own NEW src/game/cityWeather.ts and src/game/cityWeatherView.ts (names flexible) and focused meaningful weather tests. You may make minimal integration edits to src/game/cityScene.ts, src/state/store.ts, existing Settings UI/preferences files as needed. Do not overwrite existing audio edits; do not modify traffic model, save serialization or unrelated files. Do not access source outside this snapshot, credential files, private saves or images. node_modules symlink is only for typechecking/tests. No subagents, git operations, network browsing or uploads. Installed-client model access is authorized for this task.

Use deterministic visual weather based on existing city elapsed time if suitable so reloads do not arbitrarily reset weather. Explain chosen provisional cycle lengths and any limitation. Weather off must remove effects immediately even when paused; resize/pan/zoom and scene cleanup must be correct. Keep title and challenges behavior deliberate and document it. Code-native Graphics fine; no new bitmap artwork.

Run focused functional tests and typecheck (npx tsc --noEmit). Return concise changed-files list and docs/weather/HANDOFF.md with implementation, validation, concerns/disagreement and proposed reusable lessons. Codex will inspect diff and integrate and verify. Build assets omitted intentionally; do not reconstruct them.
