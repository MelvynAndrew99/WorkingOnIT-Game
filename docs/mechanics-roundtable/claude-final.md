**Accept, without reservation:** payment once at completed visit; return/access/safety tracked separately from revenue; parks as real leisure destinations keeping the existing 20/10s + 15/10s household benefit; full actual-paid refunds; online idle production legitimate; no efficiency multiplier, distance bonus, or ordinary congestion fines in the first slice; and the order — trip-intent audit, then diagnostic ledger + one non-timed shared-junction mission, then capacity, bounded outside traffic, optional occupancy.

**Corrections I take:** my park "recent window" concern is wrong — non-stacking per household is already implemented, and steady maintained leisure earning continuously is a legitimate outcome, not double-billing. Withdrawn. The recovery floor is an untested hypothesis, deferred, and I do not claim it as consensus; it should not be touched before measurement. My "full refunds mean a filled map is never terminal" claim was too strong: active trips and zero-paid tutorial geometry break it, so recoverability has to be demonstrated by real edits, no-reset access, and finite recovery help rather than asserted from the refund rule. And I accept that pods are a fine early rest — shared destinations and outside demand arrive as opt-in later pressure, never as forced cross-town trips that invalidate a working layout.

**Refinements I'd fold in:** upgrade preview shows added households and destination capacity **exactly**, and labels any road-throughput figure an estimate — that's better than my original framing and I'd adopt it verbatim. Road edits only resolve a full-shop case when access genuinely was the bottleneck, which is precisely why the ledger must attribute each unfilled slot to *no path* / *queued approach* / *slot-denied* rather than reporting utilisation alone (the Cities: Skylines demand page shows utilisation with no cause; that gap is the thing worth beating).

**Still open, flagged not settled:** I'd hold that slice-1 diagnostics must be **per-neighborhood**, not city-wide averages — a starved approach has to be visible while total throughput rises. And I'd like orphaned-reservation handling (replan or refuse on demolish under a live journey) treated as an in-slice bug guard rather than deferred; since slots free on departure, the remaining leak is inbound-only and cheap to close now. I also drop `too far` as an unserved reason — no cutoff exists — but ask that served trips record chosen destination and actual travel time, so any future cutoff or decay argument is settled by data instead of assertion. I concede the first growth unlock: second neighborhood through a shared junction, not occupancy.

**Recommend first:** the trip-intent audit, scoped tightly — enumerate every live and pending trip's intended destination class and instance, then assert intent survives road edits, demolition, reload, and tutorial Skip, with no destination substitution and no silently dropped demand. No new UI, no numbers touched. Metrics built on unpreserved goals would measure the wrong city, and every later slice inherits that.
