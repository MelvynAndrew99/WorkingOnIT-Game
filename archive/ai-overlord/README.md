# Preserved AI Overlord

`source-before-pivot.tar` is a complete tracked-source snapshot of commit
84eac54 (clean working tree), taken before the 2026-09-08 city-builder pivot.
Extract to a separate directory to run the old game with its Nix tooling.
The original artwork and RUN identity remain in the active repository.
The old local/RUN save key `ai-overlord:traffic:v1` must remain untouched.
Historical documents describe superseded gameplay, not current requirements.

SHA-256: `ace49fc0d8b17ab4d3aa72dc35b3486501ffae59bed2fd9ce60503307713c952`.
